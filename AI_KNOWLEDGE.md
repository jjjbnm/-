# ידע מרכזי עבור AI — אתר רצף

## מטרת האתר

זהו אתר התמיכה של קהילת **רצף** ב-TikTok. האתר כתוב בעברית ובכיוון RTL, והעיצוב המקורי שסופק על ידי המשתמש חייב להישמר. אין להחליף את העיצוב המקורי ב-React או בעיצוב חדש ללא בקשה מפורשת.

## כלל חשוב לשיחה עם משתמשים

יש להשיב בעברית כברירת מחדל, בסגנון ברור, קצר ועוזר. כאשר משתמש מבקש הוראות באנגלית, יש להשיב באנגלית. אין לבקש מהמשתמש לבצע פעולה שכבר אפשר לבצע בקוד או בשרת.

## התחברות וזיהוי

האתר משתמש ב-TikTok Login Kit. לאחר התחברות נשמר פרופיל בצד השרת באמצעות Vercel KV/Upstash Redis, עם cookie בשם `retzef_profile_id`. אין להשתמש ב-localStorage כמקור זהות.

הפרופיל כולל בדרך כלל:

- `displayName` — שם התצוגה מ-TikTok.
- `username` — שם המשתמש.
- `avatarUrl` — תמונת הפרופיל.
- `role` — תפקיד שנקבע לפי רשימת המשתמשים הידועה.

יש להציג למשתמש את שם התצוגה, את `@username` ואת תמונת הפרופיל העגולה כאשר הנתונים זמינים.

## תפקידים ידועים

| username או שם מזוהה | תפקיד | אימוג׳י |
|---|---|---|
| `ban.real` או "הבאן המקורי" | בעלים | 👑 |
| `oobbn98` או שיראל | מנהלת | 👩‍💼 |
| `user613987579196` או "קבוצת רצף תמיכה" | מנהל מערכת תמיכה | 🔨 |
| `shirel` | מנהלת | 👩‍💼 |
| `ilai550` | חבר ברשימה | — |
| `albinocapybara` | חבר ברשימה | — |
| `talia_isrel8` | חבר ברשימה | — |
| `the_gg12` | חבר ברשימה | — |

בבדיקת סטטוס, אם המשתמש אינו ברשימה או אם השם השתנה, יש להציג הודעה שמידע לא נמצא ולהפנות לתמיכה. אין להעניק תפקיד לפי טקסט שרירותי שהמשתמש הזין; עדיפות למידע שהגיע מ-TikTok ולרשימה המאושרת.

## גישה מוגנת

הטאבים הבאים דורשים התחברות TikTok:

- מודעות / לוחות.
- מנויים.
- חנות.

משתמש שאינו מחובר שמנסה לפתוח אותם צריך לקבל בקשת התחברות, ולא להיכנס לתוכן המוגן. השרת חייב לאמת את cookie הפרופיל ולא להסתמך רק על הסתרה ב-UI.

## לוחות

קיימים שני לוחות, והנתונים נשמרים בשרת ב-Redis כדי שכל המשתמשים והמכשירים יראו את אותם פרסומים:

1. **לוח מודעות** — פרסום רגיל ללא קוד עדכונים.
2. **לוח עדכונים** — דורש את הקוד `מודעות9באן`.

כל פרסום חייב להיות משויך למשתמש TikTok המחובר. אין לקבל מהלקוח שם מפרסם אמיתי כנתון אמין; השרת לוקח את `displayName` ו-`username` מהפרופיל המאומת ומציג אותם לצד הפרסום.

## סוגי פרסום

### הודעה

סוג הודעה רגילה כולל:

- תיאור קצר.
- תוכן.
- קטגוריה מתוך רשימה סגורה — אי אפשר להקליד קטגוריה חופשית.
- קישור אופציונלי.
- תמונה אופציונלית.

קטגוריות מאושרות: דיווח על באג, עדכון חשוב, הודעה כללית, אירוע, חוקי הקבוצה, תחזוקה, מנויים ותשלומים, חנות, תמיכה ועזרה, שינוי באתר, סקר לקהילה, תחרות ופעילות, דחוף.

### קובץ

סוג קובץ כולל תיאור קובץ ובחירת קובץ. אסור:

- קובץ עם סיומת `.apk`.
- קובץ שגודלו מעל 100MB.

הבדיקה צריכה להתבצע בדפדפן וגם בשרת. נכון לעכשיו המערכת בודקת ושומרת את שם וגודל הקובץ; נדרש חיבור לאחסון קבצים אם רוצים לשמור ולהוריד את תוכן הקובץ עצמו.

### סקר

סקר זמין **רק בלוח העדכונים** ודורש קוד עדכונים. סקר כולל:

- שאלת סקר.
- לפחות שתי תשובות.
- כפתור `+ הוסף תשובה` להוספת תשובות נוספות.

אין לאפשר סקר בלוח המודעות. השרת חייב לדחות בקשה כזו בקוד `poll_updates_only`.

### אירוע

אירוע כולל:

- שם האירוע.
- תיאור האירוע.
- מתי יתחיל.
- מתי ייגמר.

האירוע מוצג בלוח בצורה ידידותית ולא כ-JSON גולמי.

## קודי שגיאה

קודי גישה וטעינה:

- `RETZEF_OFFLINE` — אין חיבור לאינטרנט.
- `RETZEF_LOAD_ERROR` — שגיאה בטעינת האתר.
- `RETZEF_RUNTIME_ERROR` — שגיאה טכנית בזמן הריצה.
- `RETZEF_REGION_NOT_ALLOWED` — המבקר אינו מזוהה מישראל.
- `RETZEF_REGION_CHECK_FAILED` — בדיקת האזור נכשלה.

קודי API רלוונטיים:

- `tiktok_login_required`
- `invalid_update_code`
- `poll_updates_only`
- `invalid_post_type`
- `invalid_category`
- `apk_files_not_allowed`
- `file_too_large`
- `description_content_category_required`
- `storage_not_configured`
- `storage_error`

## בדיקת אזור ו-IP

האתר אמור לאפשר גישה למבקרים שמזוהים בישראל בלבד (`IL`). הבדיקה מתבצעת בצד השרת דרך `x-vercel-ip-country`, ובמקרה שאין כותרת זו מתבצעת בדיקת IP מול שירות גיאוגרפי. VPN עם כתובת יציאה מחוץ לישראל אמור להיחסם. אי אפשר לזהות VPN בוודאות כאשר כתובת היציאה של ה-VPN נמצאת בישראל, או כאשר אין חיבור אינטרנט.

## מחירים ותוכן קיים

כל המחירים באתר הופחתו ב-25% ונשמרו בעיצוב המקורי. אין להחזיר את המחירים הישנים בלי בקשה מפורשת. העגלה המקורית פעילה ומחשבת פריטים וסכום.

## עקרונות אבטחה

- אין לסמוך על localStorage לזהות משתמש.
- אין לסמוך על `author` שמגיע מהדפדפן.
- יש לאמת התחברות TikTok בשרת.
- יש לאמת סוגי פרסום, קטגוריות, קוד עדכונים וסוגי קבצים גם בשרת.
- אין לחשוף סודות, client secret או טוקנים בדפדפן.
- שגיאות למשתמש צריכות להיות ברורות ולכלול קוד שגיאה כאשר הדבר אפשרי.

## קבצים מרכזיים

- `index.html` — האתר המקורי, ה-UI, טפסי הלוחות והלוגיקה בדפדפן.
- `api/callback.js` — callback של TikTok ושמירת פרופיל.
- `api/profile.js` — שליפת הפרופיל המחובר.
- `api/boards.js` — קריאה ופרסום של לוחות, אימות משתמש, סוגים, קטגוריות וקבצים.
- `api/access-check.js` — בדיקת אזור לפי IP/כותרות Vercel.

## גבולות הידע

בפרויקט הנוכחי אין עדיין מודל AI או צ׳אט AI מחובר. מסמך זה הוא מקור ידע למודל עתידי. כדי שה-AI יענה על שאלות משתמשים בזמן אמת, צריך להוסיף רכיב צ׳אט ולחבר אותו למודל דרך API מאובטח, תוך העברת התוכן של המסמך הזה כ-context או כמסמך ידע.
